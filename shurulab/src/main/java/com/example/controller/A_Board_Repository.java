package com.example.controller;

import java.util.Optional;

import org.springframework.data.jpa.repository.JpaRepository;

public interface A_Board_Repository extends JpaRepository<A_BoardEntity, Long> {
}
